<div style="display: flex; align-items: center; gap: 14px; margin-bottom: 8px;">
    <img src="/modules/xve-laravel-kit/images/logo.svg" alt="XVE" style="height: 40px; width: auto;" />
    <div>
        <h2 style="margin: 0;">XVE Laravel Kit</h2>
        <p style="margin: 4px 0 0; color: #666;">Atomic deployments with rollback, Laravel management tools, and zero-downtime release switching.</p>
    </div>
    <div style="margin-left: auto;">
        <a href="<?php echo Modules_XveLaravelKit_Url::action('index/guide'); ?>" class="btn">How It Works</a>
    </div>
</div>

<div style="margin-bottom: 24px; padding: 16px; background: #f0f7ff; border: 1px solid #b3d4fc; border-radius: 4px;">
    <h3 style="margin-top: 0;">Create Laravel Site</h3>
    <p style="margin-bottom: 12px; font-size: 13px; color: #555;">
        Creates a new Plesk subscription, configures deployment, generates SSH deploy key, and sets up the directory structure — all in one step.
    </p>
    <form method="post" action="<?php echo Modules_XveLaravelKit_Url::action('index/create'); ?>"
          onsubmit="return confirm('Create new Laravel site?');">
        <div style="display: grid; grid-template-columns: 1fr 1fr; gap: 12px; max-width: 700px;">
            <div>
                <label style="display: block; font-weight: bold; margin-bottom: 4px; font-size: 13px;">Domain name *</label>
                <input type="text" name="domain_name" required placeholder="myapp.example.com"
                       style="width: 100%; padding: 6px 8px; border: 1px solid #ccc; border-radius: 3px; font-size: 13px;" />
            </div>
            <div>
                <label style="display: block; font-weight: bold; margin-bottom: 4px; font-size: 13px;">Git repository *</label>
                <input type="text" name="git_repo" required placeholder="git@github.com:user/repo.git"
                       style="width: 100%; padding: 6px 8px; border: 1px solid #ccc; border-radius: 3px; font-size: 13px;" />
            </div>
            <div>
                <label style="display: block; font-weight: bold; margin-bottom: 4px; font-size: 13px;">Branch</label>
                <input type="text" name="branch" value="main" placeholder="main"
                       style="width: 100%; padding: 6px 8px; border: 1px solid #ccc; border-radius: 3px; font-size: 13px;" />
            </div>
            <div>
                <label style="display: block; font-weight: bold; margin-bottom: 4px; font-size: 13px;">Node.js version</label>
                <select name="node_version" style="width: 100%; padding: 6px 8px; border: 1px solid #ccc; border-radius: 3px; font-size: 13px;">
                    <?php foreach ($this->nodeVersionOptions as $val => $label): ?>
                        <option value="<?php echo $this->escape($val); ?>"<?php echo $val === '22' ? ' selected' : ''; ?>>
                            <?php echo $this->escape($label); ?>
                        </option>
                    <?php endforeach; ?>
                </select>
            </div>
            <div>
                <label style="display: block; font-weight: bold; margin-bottom: 4px; font-size: 13px;">Package manager</label>
                <select name="node_pm" style="width: 100%; padding: 6px 8px; border: 1px solid #ccc; border-radius: 3px; font-size: 13px;">
                    <option value="npm">npm</option>
                    <option value="pnpm">pnpm</option>
                    <option value="yarn">yarn</option>
                    <option value="auto">Auto-detect</option>
                </select>
            </div>
        </div>
        <div style="margin-top: 14px;">
            <button type="submit" class="btn btn-primary">Create Laravel Site</button>
        </div>
    </form>
</div>

<?php
$configured = array_filter($this->domains, function($d) { return $d['enabled']; });
$unconfigured = array_filter($this->domains, function($d) { return !$d['enabled']; });
?>

<?php if (empty($configured)): ?>
    <p>No domains configured yet. Select a domain below to get started.</p>
<?php else: ?>
    <table class="table">
        <thead>
            <tr>
                <th>Domain</th>
                <th>Status</th>
                <th>Current Release</th>
                <th>Last Deploy</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
            <?php foreach ($configured as $domain): ?>
                <tr>
                    <td><?php echo $this->escape($domain['name']); ?></td>
                    <td>
                        <?php if ($domain['lastStatus'] === 'success'): ?>
                            <span style="color: green;">&#x2705; Active</span>
                        <?php elseif ($domain['lastStatus'] === 'failed'): ?>
                            <span style="color: red;">&#x274C; Failed</span>
                        <?php else: ?>
                            <span>&#x2796; Configured</span>
                        <?php endif; ?>
                    </td>
                    <td><?php echo $this->escape($domain['currentRelease'] ?: '—'); ?></td>
                    <td><?php echo $this->escape($domain['lastDeploy'] ?: '—'); ?></td>
                    <td style="white-space: nowrap;">
                        <a href="<?php echo Modules_XveLaravelKit_Url::action('domain/index', ['domain_id' => $domain['id']]); ?>"
                           class="btn">
                            Manage
                        </a>
                        <form method="post" style="display: inline;"
                              action="<?php echo Modules_XveLaravelKit_Url::action('domain/delete', ['domain_id' => $domain['id']]); ?>">
                            <button type="submit" class="btn"
                                    onclick="return confirm('Remove configuration for <?php echo $this->escape($domain['name']); ?>? Settings only, files on disk are kept.');">
                                &#x1F5D1; Remove
                            </button>
                        </form>
                    </td>
                </tr>
            <?php endforeach; ?>
        </tbody>
    </table>
<?php endif; ?>

<?php if (!empty($unconfigured)): ?>
    <h3>Available Domains</h3>
    <table class="table">
        <thead>
            <tr>
                <th>Domain</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
            <?php foreach ($unconfigured as $domain): ?>
                <tr>
                    <td><?php echo $this->escape($domain['name']); ?></td>
                    <td>
                        <a href="<?php echo Modules_XveLaravelKit_Url::action('domain/settings', ['domain_id' => $domain['id']]); ?>"
                           class="btn">
                            Configure
                        </a>
                    </td>
                </tr>
            <?php endforeach; ?>
        </tbody>
    </table>
<?php endif; ?>
